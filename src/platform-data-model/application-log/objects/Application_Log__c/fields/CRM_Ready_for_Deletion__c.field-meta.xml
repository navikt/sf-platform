<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>CRM_Ready_for_Deletion__c</fullName>
    <description>Formula field used in the deletion policy for the application log. All logs older than one month are to be deleted.</description>
    <externalId>false</externalId>
    <formula>OR(DateValue(CreatedDate) &lt; ADDMONTHS(TODAY(),-1),
			AND(DateValue(CreatedDate) &lt; TODAY() - 2, Log_Level__c == &apos;Info&apos;)
			)
			
/*Deletes all info logs older than two days and all other types older than last month */</formula>
    <formulaTreatBlanksAs>BlankAsZero</formulaTreatBlanksAs>
    <label>Ready for Deletion</label>
    <trackTrending>false</trackTrending>
    <type>Checkbox</type>
</CustomField>
